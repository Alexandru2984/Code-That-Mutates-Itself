# EvolvingMinds Core

The standalone core library containing the simulation logic (Entity, MutationEngine, EvolutionEngine, Memory, World) without the Phoenix web layer. Used for testing and experimentation outside of the LiveView UI.

